<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dailyjournal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DailyJournal">
        <activity android:name=".ReviewEntry"></activity>
        <activity
            android:name="com.example.com.github.gavinlabrec.dailyjournal.NewEntry"
            android:label="@string/title_activity_new_entry"
            android:theme="@style/Theme.DailyJournal.NoActionBar"
            tools:ignore="MissingClass" />
        <activity android:name="com.example.com.github.gavinlabrec.dailyjournal.MainActivity"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>